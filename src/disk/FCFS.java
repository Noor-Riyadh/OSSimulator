package disk;

import model.DiskSchedulingResult;

import java.util.*;

public class FCFS {

    public static DiskSchedulingResult simulate(int initialHead, int[] requests) {
        // Steps:
        // 1. Create a List<Integer> called order
        // 2. Add initialHead as first element
        // 3. Add each request in the exact order they appear in requests[]
        // 4. Call result.setHeadMovementOrder(order)
        return null; // replace this
    }
}
